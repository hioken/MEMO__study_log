# 定義
## OS
- OSとはKernelを動かし、それを人間向けのUIと繋げるためのフレームワーク
- コンピューターの実態はCPU
- どんなシステムも、最終的に命令を機械語までレイヤーを落としたうえで、Kernelに送り、CPUに動作させる過程を挟む

## ユーザー空間
### プロセス
- **プロセスの本質**
  - OSが割り当てた、他から厳重に隔離された「専用のメモリ空間（データの配置場所）」。
  - 言語（Lua, JS等）や機能（DOM操作等）といった人間側の論理的な境界に依存する単位ではなく、OSによる物理的なメモリ管理の単位である。

- **CPUの処理原則（物理的実態）**
  - CPU自体はプログラムの文脈やプロセスの境界を一切理解していない。
  - 単に指定されたメモリアドレスから機械語命令を読み出し、連続的かつ機械的に実行しているに過ぎない。

- **空間を隔離する最大の目的（メモリ保護）**
  - 異なる処理のデータや命令がメモリ上で混在し、相互干渉を起こすのを防ぐため。
  - 不正なメモリアクセスやポインタの誤参照（例：別の処理のデータ領域の読み書き）による致命的なクラッシュ（セグメンテーション違反など）をOSレベルでブロックする。

- **ライフサイクルの独立性**
  - 処理の開始時にOSからリソース（メモリ等）が割り当てられ、終了時にはOSによって当該領域が確実に解放される。
  - 各々のプロセスが独立して生成・破棄されるため、単一のプロセスが終了・異常停止しても、他のプロセスやOS全体の動作は保護される。

### スレッド
#### 概要
- Thread: 処理の細かい単位、スタック領域などの、プロセスと紐づいた情報が保存されている
- MainThread: コンパイラやインタプリタのエンジン等
#### state
- New: 生成されたばかり
- Ready: 実行可能、CPUの空き待機
- Running: 実行中
- Waiting/Blocked: 別条件が揃うまで待機している状態
- Terminated: 役割を追え、破棄される状態

#### Queue
- Queue: メモリ上に展開されている、全プロセスの共用、Kernelに管理されている
- ReadyQueue: Ready状態のThreadが並ぶ
- WaitQueue: Waitingのスレッドが並ぶ
- Concurrency: 時間のかかるThreadを一度放置して、他のThreadにCPUコアを貸し出す
1. Threadがスタック領域等の情報と共にQueueに並ぶ
2. CPUコアを一瞬貸し出されて演算を行う
3. 終了次第解散

## Kernel空間
- メモリ上に、権利が強力な単一のプロセスの用に展開されている
- ユーザー空間のシステムとは別に、KernelThreadQueueにThreadが立てられる


# システムコール
## open
### 1. 誤解されがちなメンタルモデル（初期の仮説）
* 指定ファイルがメモリ（ページ）に展開・確保される。
* 実行プロセスに対して、そのメモリ領域へのアクセス権限を付与する。
※ 実際のところ、この直接メモリアクセスを許可する挙動は `open` ではなく **`mmap`** システムコールの役割である。

### 2. 実際の `open` の役割（遅延評価と契約手続き）
カーネルは「必要な時までデータは動かさない」という原則を持つ。そのため、`open` 時点では**ファイルの中身（データ）は一切メモリに読み込まれない**。行われるのは以下の3ステップのみである。

1. **パスの解決と権限チェック**
   * 指定パスの存在確認と、要求されたモード（Read/Write等）に対するプロセスの操作権限を厳密に審査する。
   * ※ `/proc/self/statm` の場合、**このパス解決の瞬間に `self` が呼び出し元の実際のPID（例: `/proc/9999/statm`）にすり替わり**、対象プロセスが固定される。

2. **管理構造体（`struct file`）の作成**
   * カーネル空間にファイル操作の状態を記録するカルテを作成する。
   * ここには、次回どこから読み取るかを示す**「ファイルオフセット（先頭からのバイト数によるカーソル位置）」**などが保持される。

3. **ファイルディスクリプタ（FD）の発行**
   * ユーザー空間のプロセスに対し、作成した管理構造体にアクセスするための単なる整数値（FD ＝ 整理券）を返す。

### 3. データの実体へのアクセス（`read` の役割）
プロセスがファイルデータに直接アクセスすることは許されておらず、必ずカーネルを介す必要がある。

* プロセスは `read` コールを用いて、FDと受け皿（バッファ）を指定し、データの取得を依頼する。
* カーネルは、FDに紐づくファイルオフセット（カーソル位置）から指定バイト数だけデータを読み出し、プロセスのバッファへ**コピー**して渡す。
* 渡し終えたバイト数分だけ、自動的にファイルオフセット（カーソル）を後方へ進める。